.First <- function(){
    if(interactive()){
        library(dataview)
        ev <- entry.view
        tv <- tree.view
    }

    options(repos="http://ftp.sunet.se/pub/lang/CRAN/")
    if(file.exists("~/.Rpatches"))
        source("~/.Rprofile")
}

